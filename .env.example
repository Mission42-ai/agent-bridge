# === HTTP Transport (always active) ===
BRIDGE_HTTP_PORT=3099
BRIDGE_HTTP_HOST=127.0.0.1
BRIDGE_HTTP_BEARER_TOKEN=
# BRIDGE_HTTP_NO_AUTH=true  # Dev only — disables auth requirement

# === Optional: Pusher Transport (only when PUSHER_APP_KEY is set) ===
# PUSHER_APP_KEY=
PUSHER_CLUSTER=eu
PUSHER_CHANNEL=agent-bridge
PUSHER_EVENT=run

# === Bridge Config ===
BRIDGE_BASE_DIR=/tmp/agent-bridge
BRIDGE_OVERLAYS_DIR=./overlays
BRIDGE_TIMEOUT=1800000
MAX_CONCURRENT=3
BRIDGE_DEFAULT_PROVIDER=claude-code
# BRIDGE_DEFAULT_MODEL=sonnet
# BRIDGE_SYSTEM_PROMPT_SUFFIX=

# === Optional: Memory Bridge (configurable-es platform) ===
# CES_BASE_URL=https://your-instance.vercel.app
# CES_TENANT_ID=
# CES_APP_ID=
# CES_API_KEY=
# MEMORY_MAX_CONTEXT_TURNS=50

# === Optional: GitHub Token (for private repos) ===
# GITHUB_TOKEN=
